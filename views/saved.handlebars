<div class="container-fluid">
    <div class="row">
        <div class="col-sm-6">
           {{#each articles}}
           {{#if saved}}
           {{> article}}
           {{/if}}
           {{/each}}
        </div>
    </div>
</div>